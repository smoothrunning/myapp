<h1>Editing page</h1>

<% form_for(@page) do |f| %>
  <%= f.error_messages %>
 <p>
    <%= f.submit "Update" %>
  </p>
  <%= render :partial => 'form', :locals => {:f => f} %>
   
<% end %>

<%= link_to 'Show', @page %> |
<%= link_to 'Back', pages_path %>
